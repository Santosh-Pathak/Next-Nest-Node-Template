/**
 * @deprecated Import from `@/components/guards/AuthGuard` instead.
 * Re-export kept to avoid breaking accidental imports (ISP / DRY).
 */
export {
   AuthGuard,
   withAuth,
   RoleGuard,
   GuestGuard,
   RequireAuth,
} from '@/components/guards/AuthGuard'
