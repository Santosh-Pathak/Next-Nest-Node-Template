# Frontend

Next.js App Router starter for the Next + Nest template.

## Setup

```bash
cp .env.example .env.local
npm install
npm run dev
```

## Env

See `.env.example`. Primary variable:

- `NEXT_PUBLIC_API_BASE_URL` — backend base URL (default `http://localhost:3001`)
